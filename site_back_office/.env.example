# Hood Family Farms Frontend Configuration
# Copy this file to .env and update values as needed

# API URL - Backend server address
# In development, the proxy in package.json handles this
# In production, set the full URL to your API
REACT_APP_API_URL=http://localhost:3001/api/v1

# App name (optional)
REACT_APP_NAME=Hood Family Farms

# Environment
REACT_APP_ENV=development
